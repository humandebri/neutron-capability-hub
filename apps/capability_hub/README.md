# Capability Hub

Redacted control plane for installed Neutron apps, live tool endpoints, and
recent audit activity. It stores no credential and opens the dedicated Driver,
Tasks, Cycles Checkout, or x402 Guard tile for configuration.

```sh
npm --workspace neutron-capability-hub test
```
